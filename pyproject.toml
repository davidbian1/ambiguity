[project]
name = "ambiguity"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "david.bian@duke.edu" }
]
requires-python = ">=3.13"
dependencies = [
    "langgraph==1.2.11",
    "langsmith>=0.7.7",
    "numpy==2.5.2",
    "openai==3.7.0",
    "python-dotenv==1.2.3",
    "pyyaml==6.0.3",
    "scikit-learn==1.9.0",
]

[project.scripts]
ambiguity = "ambiguity:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
